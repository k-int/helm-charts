apiVersion: v1
kind: Service
metadata:
  name: ki-selfhost-operator
  labels:
    app.kubernetes.io/name: ki-selfhost-operator
spec:
  selector:
    app: ki-selfhost-operator
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
